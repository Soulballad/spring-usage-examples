server:
  port: 14011

dubbo:
  registry:
    address: spring-cloud://localhost
  cloud:
    # subscribed provider service
    subscribed-services: ${provider.applicaton.name}

  consumer:
    check: false

provider:
  applicaton:
    name: alb-dubbo-provider
