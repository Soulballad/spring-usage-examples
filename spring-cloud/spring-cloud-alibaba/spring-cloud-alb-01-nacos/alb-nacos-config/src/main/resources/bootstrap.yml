server:
  port: 14000

spring:
  application:
    name: alb-nacos-config
  cloud:
    nacos:
      config:
        # nacos 地址
        server-addr: 127.0.0.1:8848
        # 指定配置文件类型
        file-extension: properties